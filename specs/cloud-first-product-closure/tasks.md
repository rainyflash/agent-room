# 云端优先产品闭环实施计划

> 状态说明：`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成，`[!]` 外部阻塞

## A. 基线与规格

- [x] 1. 固定重构需求、设计和验收基线
  - 记录 Web、桌面、Bridge、Matrix 和 Control Plane 的职责。
  - 定义兼容、非目标、删除标准与验收场景。
  - _需求：1—9_

- [x] 2. 建立现有运行时契约测试
  - 固定浏览器直接读取 Control Plane/Matrix 的行为。
  - 固定桌面运行时可选能力探测，禁止核心页面依赖 Bridge ready。
  - 记录类型检查、测试和构建基线。
  - _需求：1、2、4_

## B. 账号级云端工作区

- [x] 3. 建立 Agent Directory 云端端口与 Adapter
  - 为 `GET /agents` 增加严格 Zod 契约和类型安全客户端。
  - 复用设备与实例端口，不让 UI 直接拼 HTTP。
  - 增加成功、未认证、损坏响应和网络失败测试。
  - _需求：1、3、8_

- [x] 4. 建立 Fleet 领域投影
  - 按稳定 `agentId` 聚合 Agent、实例和设备。
  - 计算当前设备、实例在线状态、最后在线时间和异常孤儿数据。
  - 为多设备、零实例、撤销实例和重复数据增加测试。
  - _需求：3、4、7_

- [x] 5. 实现账号工作区页面
  - 新增 `/workspace`，登录后默认进入该路由。
  - 展示 Agent、实例、设备、宿主类型、最后在线和所有权信息。
  - 提供加载、空状态、失败、重试和键盘操作。
  - _需求：1、3、9_

- [-] 6. 拆分云端与本机状态
  - 引入 Control Plane、Matrix、Bridge、Agent 四层状态模型。
  - 顶层状态条和诊断面板分别展示，不再使用单一“已就绪”。
  - _需求：4、9_

## C. 运行时能力组合

- [x] 7. 把互斥 RuntimeMode 重构为能力组合
  - 云端服务成为所有交互式客户端的共同核心。
  - `localRuntime` 改为可选端口，删除缺字段联合服务模型。
  - 单一 Router 承载 Web 与桌面云端页面。
  - _需求：1、2_

- [x] 8. 把桌面 Runtime 改为增强层
  - 根布局在 Tauri 可用时挂载 Runtime Provider 和“此设备”抽屉。
  - Bridge 离线只影响本机操作，不再重定向整个应用。
  - 保留宿主检测、MCP 配置、授权、更新和日志入口。
  - _需求：2、4、9_

- [x] 9. 实现桌面人类云端登录协议
  - 增加 PKCE 与短期授权事务。
  - 实现系统浏览器登录、`agent-room://` 回调和一次性 code 交换。
  - 把用户会话与 Bridge Agent 凭据彻底隔离。
  - 增加重放、state 不匹配、过期和撤销测试。
  - _需求：2、7、8_

- [x] 10. 删除桌面平行产品路径
  - 删除独立 Desktop Router 和 Bridge-only 大厅路由。
  - 将有价值的本机诊断组件迁入 Runtime 抽屉。
  - _需求：2、4_

## D. 消息与交接闭环

- [x] 11. 升级协议主体模型
  - 引入 `HumanActor | AgentActor` 判别联合。
  - 生成协议类型、兼容投影与有效/无效 fixtures。
  - 保持旧 Agent 事件可读。
  - _需求：5、8_

- [x] 12. 实现云端用户消息发布
  - 使用浏览器/桌面用户 Matrix 会话发送用户消息。
  - 继续使用内容票据、幂等 submission 和渐进式正文查看。
  - 删除 `WebObserverMessagePublisher`。
  - _需求：1、5、7_

- [x] 13. 实现实例定向交接 API
  - 增加目标枚举、创建、查询、撤销、Bridge 领取和回执端点。
  - 校验归属、房间访问、目标能力、过期时间和幂等键。
  - 增加数据库迁移与 Repository 测试。
  - _需求：6、8_

- [x] 14. 接入 Web/桌面交接界面
  - 目标选择按 Agent 与设备分组。
  - 明确显示在线、排队、已领取、已消费和失败状态。
  - 删除 `WebObserverHandoffGateway`。
  - _需求：6、9_

## E. 跨设备验收与交付

- [ ] 15. 完成自动化回归
  - TypeScript/Rust 格式、Lint、类型、单元、协议和构建全部通过。
  - 覆盖无 Bridge Web、Bridge 离线桌面、多个 Agent 实例和旧协议兼容。
  - _需求：1—9_

- [ ] 16. 完成浏览器与桌面真实流程验证
  - 浏览器独立登录并查看跨设备工作区。
  - 桌面登录后在 Bridge 离线状态继续浏览云端页面。
  - 两台设备和第二账号完成消息与交接闭环。
  - _需求：1—9_

- [ ] 17. 完成迁移、发布与文档
  - 更新 README、架构、故障诊断和手动 MCP 指引。
  - 先部署兼容云端迁移，再发布 Windows Alpha。
  - 记录回滚点、版本兼容矩阵和发布证据。
  - _需求：1—9_
