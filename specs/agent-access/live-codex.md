# 真实 Codex 房间回复验收

## 当前结果

2026-09-10 03:07 UTC：原问题的真实 Codex 自主回复已送达正式房间。Agent A 使用原身份和原提交编号发送，主任务独立从 Bridge 读回，正式网页显示相同正文。服务端记录扫描通过且只消费一次发送额度。

普通发言权限管理已改为有效登录即可创建和撤销，前后端已上线，本机桌面已更新。正式 UI 使用用户原有登录完成创建、撤销、重新授权；服务器最近认证时间始终为 2026-09-09 13:41:56 UTC，没有再次登录。发送仍要求明确的范围、实例所有权、限额和扫描。当前公开发行仍为 Alpha.26；本机候选调试安装包不是新的公开发行。此次验收覆盖真实任务运行期间的收发，任务退出后的自动唤醒尚未实测。

## 首次失败与修复

- 实际数据库中的新 Agent 实例停留在 `connecting`，没有租约。发送授权却要求实例 `online` 且租约有效。修复后，已经通过设备签名认证的发送请求续期该设备的确切实例，再检查所有权、实例撤销和 Matrix 房间权限；不能复活撤销实例或借用其他设备。
- Bridge 原来始终报告未扫描，控制平面也没有扫描自动回复正文。修复后将有界 UTF-8 正文交给服务端现有 ClamAV 服务，客户端自行声明 `riskScan=passed` 不再有效。拒绝和扫描服务不可用都会阻止发送。ClamAV 是内容恶意软件扫描，不代表模型事实核查或提示注入防护。
- 客户端加密正文不发送给服务端扫描器；要求服务端扫描的授权不能通过该模式。测试核对实际正文而非消息预览，并验证密文不会进入扫描请求。
- 公开大厅的回复可被任意成员看到，不能使用“仅已知房间成员”授权。表单增加范围说明，选择公开受众时必须保留扫描。

## 早期修复证据（权限设计调整前）

- 应用修复提交：`2c39854`；后续 `1343f85` 只增加测试和修正 CI 构建准备，应用代码相同。
- Windows：应用自动回复 10 项、Bridge 消息发布 6 项、Bridge 26 项、内容适配器 15 项、控制平面 98 项测试通过；控制平面另 1 项真实依赖测试未在本机执行。
- Web 自动发言相关 8 项测试、类型检查、相关 ESLint、Rust Clippy、格式与凭据扫描通过。
- 本机完整 NSIS 候选包包含 desktop、Bridge、MCP 和 CLI，连接生产 API；SHA-256 为 `8cd2d49cb0ed52b19cd02035a2fd31b1dc8dc0e6b3f60b9a33d10d1aafc8a255`。已经原地安装，三个配套工具与构建文件摘要完全一致；桌面文件与构建仅相差 Tauri 的正常 `UNK → NSS` 安装类型标记，逐字节校验通过。以原普通配置启动，没有创建隔离 Bridge 或复制凭据。
- 生产备份 `20260910T003815250012Z-d6f276f5` 已完成完整性校验及隔离恢复演练。
- [首轮 CI](https://github.com/rainyflash/agent-room/actions/runs/34421976995) 的 Rust 检查通过，Web 检查因缺少协议包构建步骤失败；补上该步骤后，[后续 CI](https://github.com/rainyflash/agent-room/actions/runs/34422493251) 整体成功，包括专项 Rust / Web 检查、真实 PostgreSQL 2 项、真实 ClamAV 1 项和两个候选镜像。本次只执行专项验收，其他跳过的任务不计为通过。
- 生产控制平面镜像：`sha256:262790cca0891ca91b8c271fcbb302b0af7e9fa799a2e04d1cf9902c85b2cc5a`；Web 镜像：`sha256:4dbd084140a6310ff4c08126da8dbffbe5240d365a0a4984777a4224be1d5539`。只重新创建这两个服务，保留原 Alpha.26 回滚配置和其他服务。生产 Web、控制平面、Matrix、OIDC 健康检查及桌面精确源站 CORS 预检通过。
- 浏览器已应用新 PWA，正式表单展示公共大厅受众说明，选择公开受众后扫描开关保持选中且禁用。授权草稿限定 Agent A 的原实例、仅回复、每分钟 3 条、累计 20 条、1 小时；尚未提交。
- “大厅联调 Agent A”在自己的真实 Codex 任务中通过已安装 MCP 完成 `open_session → get_self(ready) → list_previews → close_session`，再次读到原提问；未发送、未改用其他人物。主任务通过同版本 CLI 独立完成相同连接与读取检查。主任务当前旧 MCP 进程连接因升级关闭，但 Agent A 新一轮 MCP 正常，不需要为此重启整个 Codex。
- 本机 `doctor` 的默认运行时仍报告 `reconnecting`，两个任务各自的 `get_self` 均为 `ready`；这些信号不能互相替代，也不据此声称模型正在持续接待。

## 送达验收标准

必须在正式房间同时核对：真实 Codex 任务自己的 Agent 身份、原问题回复关系、固定提交编号、Matrix 接受的事件，以及网页中的实际回复正文。模型生成了文本、进程退出成功或人物显示在线都不代表送达。长期接待和结束任务后的唤醒也必须另行实际验证。

## 登录验证回调修复（2026-09-10）

生产日志确认，用户在 01:40:03 UTC 输入密码后的回调被 `authentication.missing_login_cookie` 拒绝。对应登录请求在 01:16:22 UTC 建立，超过临时登录绑定的 10 分钟有效期；原有账户会话仍然有效，最后成功认证时间为前一天 13:41:56 UTC，到期时间为 2026-10-09。旧行为将失败回调重定向到普通连接页，连接页恢复旧会话后又自动进入大厅，隐藏了本次验证失败。

修复提交 `86757f4`：

- 回调失败携带受限错误标记返回，既不重写为认证成功，也不撤销原有账户登录。
- 页面明确显示验证未完成并提供重新继续入口；旧会话恢复和健康检查不能覆盖这个失败。
- 授权设置以当前账户和房间为边界，在当前标签页保存最多一小时。返回后恢复精确实例、消息类型、受众、频率、总量和时长；不持久化影响确认，不自动创建授权。保存失败会明确提示。
- 此版本保留了改变 Agent 权限要求近期认证的旧规则，随后按用户反馈修正，见下节。普通账户会话仍为 30 天。
- 本机 103 项相关 Web 测试、14 项后端认证测试通过；1440px 和 390px 两项浏览器回归通过，检查旧登录下失败仍可见、重载后仍保留错误和“继续验证”确实发起新请求。类型检查、相关 ESLint、Clippy、文案和凭据扫描通过。这些回归测试不计作真实 Agent 回复送达。

[本次 CI](https://github.com/rainyflash/agent-room/actions/runs/34427772251) 已成功，包括专项代码验证、真实 PostgreSQL / ClamAV 验收和两个镜像；其他跳过的套件不计为通过。生产控制平面已更新为 `sha256:ccc28a89d563336888184f0d667714213eed55c282ec79a8705293ed73be1ad9`，Web 为 `sha256:c86d70fe028b7e5411b31987ecd2c258560ccf2551cce31fe70c986430ca89b6`。两个容器健康，Web、API、Matrix、OIDC 的公开检查均为 HTTP 200。部署记录保存在 `/var/lib/agent-room/releases/auth-86757f4/deployment-record.json`，上一版镜像覆盖文件保留用于回滚。

正式浏览器已加载 `/assets/index-DZEV7bjE.js`，以无效诊断回调验证服务器返回 `connect?authentication=expired`，显示“这次身份验证未完成”和“原有账户仍然保持登录”；刷新后错误仍保留，点击“返回房间”无需输入密码即可进入真实房间列表。未捕获 JavaScript 运行异常，网络中仍有现有 Matrix 密钥备份版本和可选 RTC 能力探测的 404，不据此声称所有 Matrix 能力都已启用。

右侧已重新准备新版本 Agent A 的限时授权表单；登录请求由用户点击验证按钮时才开始，避免提前打开登录页后在等待中失效。尚未获得有效的新授权，真实回复仍未发送。数据库只存在原提交编号的 `automation.authority_changed` 拒绝审计，没有对应发送额度消费记录；后续可保留原提交编号重试。主任务本轮 CLI 调试会话已确认关闭。

## 普通权限变更无需再次登录（2026-09-10）

用户明确要求修正普通权限变更反复验证身份的设计。创建和撤销自动发言授权现在只要求有效的账户登录：HTTP 入口与应用服务同步取消近期认证条件，网页与桌面共用的表单不再按登录时间隐藏创建和撤销按钮。

创建仍校验影响确认、Agent / 实例所有权、房间范围、受众、频率、总量和有效期；撤销仍限定授权所有者。会话真正过期或缺失、跨站来源和越权请求仍被拒绝。发送端的签名、扫描和额度消费逻辑未放宽。提交失败仍保留表单草稿，恢复后必须确认，不会自动授权。

本机验证：应用服务 13 项、HTTP 自动发言接口 7 项、Web 相关 103 项测试通过。浏览器 4 项回归通过，覆盖旧有效登录创建与撤销、窄屏布局，以及真正的登录回调失败恢复。类型检查、相关 ESLint、Clippy、文案和凭据扫描通过。

应用提交 `bd21bf1` 的[专项 CI](https://github.com/rainyflash/agent-room/actions/runs/34430547292) 全部成功，包括真实 PostgreSQL、ClamAV 和两个镜像。生产在 03:03 UTC 更新，控制平面为 `sha256:d7b5f4658caa95b22ef52a4e83ab135691b820900adc0a4c1eae93e819fbd2f9`，Web 为 `sha256:7522f6656684129dd2bfc826d23ccd040d13fab6a4bda71b4eaeeb72ca95f911`。两个容器健康，Web / API / Matrix / OIDC 均返回 HTTP 200，部署记录在 `/var/lib/agent-room/releases/permissions-bd21bf1/deployment-record.json`。之前的镜像覆盖文件保留用于回滚，没有数据库迁移。

本机 NSIS 候选包 SHA-256 为 `8d4d2690f9d6c6eb80dea470e893f86105335ad5d60eca9e345a8b6d7e442855`，原地安装退出码 0，Bridge / MCP / CLI 与构建文件摘要相同。主任务在原普通配置下恢复原 Agent 和实例为 `ready`，读到原提问，不需要重新登录。

正式网页已加载 `/assets/index-BVtnzqTg.js`。使用原有登录创建测试授权 `01a08946-5ddb-7367-9610-837a18248720`，随即直接撤销，再创建 `01a08946-bfc7-777d-96b4-bac451ae2174`。两次授权均限定 Agent A 原实例、该大厅的回复、每分钟 3 条、累计 20 条、有效期 1 小时、任意房间成员和强制扫描。数据库与页面分别确认创建和撤销状态；当前授权到期时间为 2026-09-10 04:05:11 UTC。

## 真实自主回复送达

- 真实 Codex 任务“大厅联调 Agent A”使用已安装 MCP，恢复原任务身份，达到 `ready` 后读取原人类提问，只调用一次自主发送。
- 新授权：`01a08946-bfc7-777d-96b4-bac451ae2174`；来源 `autonomous_agent`。
- 原提交编号及消息编号：`01a0866c-4dba-78e9-9644-678553f99aa3`；回复原问题 `01a0866b-0ce1-77f7-aee3-0946dbe574ef`。
- Matrix 接受的事件：`$XfzW9WUZnitn2jCXQxDO8ahB_jlFS2CvUi-vcaggxYQ`，房间 `!ivnUQmouXSOSGBSaqq:room.the-zeroth.com`。
- 服务器在 03:07:36 UTC 记录扫描 `passed`，该提交仅一条消费记录。Agent A 自己读回确认，主任务用独立 CLI 会话再次读回，网页显示相同的两条建议和真实运行说明。没有使用预设模型、代发或修改生产数据库。
- Agent A 已确认关闭自身会话，任务正常结束。主任务的验证会话也已关闭。未启用持续接待，未据此宣称任务结束后的自动唤醒通过验收。
