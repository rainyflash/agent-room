# 任务 39 验证记录：容量压测与性能预算

## 1. 当前结论

六个真实容量场景的工具与严格汇总门已经实现。数据库、Matrix、内容、联邦回填和 Web 场景均产生过通过证据；2026-08-26 的完整 30 分钟双 Synapse 断网回填与当前 Web 生产构建预算均通过。

任务 39 仍为 **No-Go**。72 小时活跃 Bridge 常驻观测没有执行，数据库/Matrix/内容的已有证据也不是当前 Git 修订。严格门正确拒绝把跨修订结果拼成一个发布结论。

## 2. 已测容量

| 场景 | 真实依赖 | 已测结果 | 发布资格 |
| --- | --- | --- | --- |
| 目录与分配 | PostgreSQL 18 | 10,000 Agent、1,000 在线实例、250 人大厅通过 | 单场景通过，旧修订 |
| 大厅消息 | Synapse Client API | 250 成员；持续 10.006 条/秒；突发 63.45 条/秒；650 事件全量送达 | 单场景通过，旧修订 |
| 内容并发 | SeaweedFS S3 路径 | 4 个并发 25 MiB 对象；上传 P95 910.236ms；下载 P95 361.537ms | 单场景通过，旧修订 |
| 联邦回填 | 两个独立 Synapse/PostgreSQL | 对端离线 1,800 秒；10/10 按序回填；0 重复；回填 1,054.357ms | 当前修订通过 |
| Web 场景 | 生产 Vite + Chromium WebGL/CDP | 200 节点；P95 13.3ms；约 234 MiB JS 堆；26 资源；5 纹理 | 当前修订通过 |
| Bridge 常驻 | Release Bridge 进程 | 仅做过 0.005 秒无活跃会话探针 | 不通过 |

联邦实验的可提交摘要见 [`evidence/task-39-federation-outage.json`](./evidence/task-39-federation-outage.json)。完整一次性报告写入 ignored 的 `artifacts/capacity/`。

## 3. 30 分钟联邦断网证据

在 `agent-room-federation-test` 隔离 Compose 项目中停止 beta Homeserver 1,800 秒。alpha 在此期间继续接受本地房间写入，并按 3 分钟间隔接受 10 个待联邦事件。beta 恢复后：

- 交付率 100%；
- 事件顺序与发送顺序完全一致；
- 重复事件数为 0；
- 对端重启约 4.993 秒；
- 重新上线后的回填约 1.054 秒；
- 下一阈值为 60 分钟与 100 个排队事件。

测试结束后脚本删除其专用容器、网络和数据库卷，不触碰开发或生产项目。

## 4. Web 生产预算

内存和资源体积只在 `AGENT_ROOM_CAPACITY_REPORT=1` 的生产预览中判定；Vite 开发服务器的 HMR 模块与并行测试 worker 不是生产容量证据。当前生产结果：

- 帧耗时中位数 9.3ms，P95 13.3ms；
- JS 堆 245,787,088 字节，预算 256 MiB；
- 解码资源 2,004,884 字节，预算 12 MiB；
- 26 个资源，预算 80；
- 200 个渲染节点、5 个纹理、0 个外部图片。

## 5. 严格门结果

`python tools/capacity.py gate ...` 在 `f5867fe` 上明确返回失败：

1. 数据库、Matrix、内容与 Bridge 报告不是当前修订；
2. Bridge 72 小时报表未通过；
3. Bridge 报表不具备发布门资格。

门禁要求六份报告均来自同一精确修订、使用真实依赖、`passed=true` 且 `releaseGateEligible=true`。算法模型永远不能满足该门。

## 6. 解除条件

1. 为一个已完成授权和登录的真实 Agent/大厅会话构建 Release Bridge；
2. 连续运行 259,200 秒，每 30 秒采样，最大 RSS ≤ 512 MiB、增长 ≤ 128 MiB；
3. 在同一候选修订重跑数据库、Matrix、内容、联邦和 Web 场景；
4. 严格汇总门通过并保留原始报告。

72 小时就是 72 小时。缩短测试、伪造时间戳或用空闲进程替代活跃会话都不算完成。
