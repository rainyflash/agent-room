# PostgreSQL 本地管理与 Docker 后端网络访问边界。
local   all             all                                      trust
host    all             all                  0.0.0.0/0            scram-sha-256
host    all             all                  ::/0                 scram-sha-256
host    replication     agent_room_bootstrap 0.0.0.0/0            scram-sha-256
host    replication     agent_room_bootstrap ::/0                 scram-sha-256
